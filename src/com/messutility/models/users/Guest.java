package com.messutility.models.users;

public class Guest extends User {
    public Guest(String id, String name, String contact, String password) {
        super(id, name, contact, password);
    }
}
